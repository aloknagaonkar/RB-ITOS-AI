# Copy to .env; credentials stay on the backend.
DATABASE_URL=sqlite:///./data/lab.db
# PostgreSQL alternative (docker compose up -d db):
# DATABASE_URL=postgresql+psycopg://lab:local_lab@localhost:5432/market_lab
UPSTOX_ACCESS_TOKEN=
